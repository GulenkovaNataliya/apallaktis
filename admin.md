# АПΑΛΛΑΚΤΗΣ — Админ-панель

Полная документация админ-панели для управления пользователями, рефералами и VIP-статусами.

---

## Содержание

1. [Структура страницы](#структура-страницы)
2. [База данных (Supabase)](#база-данных-supabase)
3. [Статусы подписки](#статусы-подписки)
4. [Тарифные планы](#тарифные-планы)
5. [Функционал](#функционал)
6. [Дизайн-система](#дизайн-система)
7. [API Endpoints](#api-endpoints)
8. [Компоненты](#компоненты)

---

## Структура страницы

**Путь:** `/[locale]/admin`
**Файл:** `frontend/app/[locale]/admin/page.tsx`
**Доступ:** только для `role: "admin"`

### Макет

```
┌─────────────────────────────────────────────────────────────┐
│  HEADER (sticky)                                            │
│  [Logo]                                        [Выход]      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                   │
│  │Всего│ │Актив│ │Read │ │DEMO │ │Basic│  ... (10 карточек)│
│  └─────┘ └─────┘ └─────┘ └─────┘ └─────┘                   │
│                                                             │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 👥 Пользователи                                    [▼] ││
│  ├─────────────────────────────────────────────────────────┤│
│  │ [Поиск] [Статус ▼] [План ▼] [Документ ▼] [📥 Excel]    ││
│  │ Показано: 125                                           ││
│  │ ┌─────────────────────────────────────────────────────┐ ││
│  │ │ ID │ Имя │ Email │ Телефон │ ... │ Действия        │ ││
│  │ └─────────────────────────────────────────────────────┘ ││
│  └─────────────────────────────────────────────────────────┘│
│                                                             │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 🎁 Рефералы                                        [▼] ││
│  └─────────────────────────────────────────────────────────┘│
│                                                             │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ ⭐ VIP                                              [▼] ││
│  └─────────────────────────────────────────────────────────┘│
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Секции

#### 1. Статистика (Stats Grid)
10 карточек с градиентным фоном:

| Карточка | Описание | Градиент |
|----------|----------|----------|
| Всего | Общее количество пользователей | `#11998e → #38ef7d` |
| Активные | `subscription_status = "active"` | `#4facfe → #00f2fe` |
| Only Read | `subscription_status = "read-only"` | `#fa709a → #fee140` |
| DEMO | `subscription_status = "demo"` | `#a18cd1 → #fbc2eb` |
| Basic | `subscription_plan = "Basic"` | `#667eea → #764ba2` |
| Standard | `subscription_plan = "Standard"` | `#f093fb → #f5576c` |
| Premium | `subscription_plan = "Premium"` | `#4facfe → #00f2fe` |
| VIP | `subscription_status = "vip"` | `#f5af19 → #f12711` |
| Рефералов | Пользователи с `referred_by` | `#11998e → #38ef7d` |
| Доход | Расчётный доход | `#667eea → #764ba2` |

#### 2. 👥 Пользователи
- **Фильтры:** поиск (имя/email/ΑΦΜ), статус, план, тип документа
- **Таблица:** ID, Имя, Email, Телефон, Документ, ΑΦΜ, Status, Plan, Дата, Действия
- **Действия:** 👁️ Просмотр, ✏️ Редактирование, ⭐ Быстрый VIP
- **Экспорт:** Excel (xlsx)

#### 3. 🎁 Рефералы
- **Таблица:** Реферер, Email, Приглашено, Купили, Бонус
- **Сортировка:** по количеству приглашённых (убывание)
- **Экспорт:** Excel

#### 4. ⭐ VIP
- **Таблица:** Имя, Email, VIP с, Истекает, Кто выдал, Причина, Действия
- **Действия:** ❌ Убрать VIP, ⭐ Добавить VIP
- **Экспорт:** Excel

---

## База данных (Supabase)

### Таблица: `profiles`

```typescript
interface UserProfile {
  // Идентификация
  id: string;                          // UUID (из auth.users)
  account_number: number;              // Автоинкремент ID для отображения

  // Контактные данные
  name: string;                        // Имя пользователя
  email: string | null;                // Email
  phone: string | null;                // Телефон

  // Подписка
  subscription_status: string;         // Статус: demo, active, expired, read-only, vip
  subscription_plan: string | null;    // План: null, Basic, Standard, Premium, VIP
  subscription_expires_at: string | null; // Дата окончания подписки (ISO)

  // VIP
  vip_expires_at: string | null;       // Дата окончания VIP (ISO)
  vip_granted_by: string | null;       // UUID админа, выдавшего VIP
  vip_reason: string | null;           // Причина выдачи VIP

  // ΤΙΜΟΛΟΓΙΟ (для юридических лиц)
  doc_type: string | null;             // ΑΠΟΔΕΙΞΗ или ΤΙΜΟΛΟΓΙΟ
  afm: string | null;                  // ΑΦΜ (греческий ИНН)
  doy: string | null;                  // ΔΟΥ (налоговая инспекция)
  company_name: string | null;         // Название компании
  company_activity: string | null;     // Вид деятельности
  company_address: string | null;      // Адрес компании

  // Рефералы
  referral_code: string | null;        // Уникальный реферальный код пользователя
  referred_by: string | null;          // Реферальный код того, кто пригласил
  bonus_months: number | null;         // Бонусные месяцы за рефералов

  // Системные
  role: string;                        // user или admin
  created_at: string;                  // Дата регистрации (ISO)
}
```

---

## Статусы подписки

### `subscription_status`

| Статус | Описание | Badge цвет |
|--------|----------|------------|
| `demo` | 14-дневный демо-период | `#e2e3f3` / `#383d8b` |
| `active` | Активная платная подписка | `#d4edda` / `#155724` |
| `expired` | Подписка истекла | `#f8d7da` / `#721c24` |
| `read-only` | Режим только чтение (после истечения) | `#fff3cd` / `#856404` |
| `vip` | VIP-статус (выдан админом) | gradient `#f5af19 → #f12711` |

### Жизненный цикл

```
Регистрация → demo (14 дней)
                 ↓
         ┌──────┴──────┐
         ↓             ↓
     Оплата       Не оплатил
         ↓             ↓
      active      expired → read-only
         ↓
    Истекла подписка
         ↓
      expired → read-only

В любой момент админ может выдать → vip
```

---

## Тарифные планы

### `subscription_plan`

| План | Цена (με ΦΠΑ 24%) | Описание |
|------|-------------------|----------|
| `null` | Бесплатно | DEMO (14 дней) |
| `Basic` | €24,80/год | Базовый функционал |
| `Standard` | €49,60/год | Расширенный функционал |
| `Premium` | €93,00/год | Полный функционал |
| `VIP` | Бесплатно | Выдаётся админом |

### Badge стили

```typescript
const getBadgeStyle = (type: string): React.CSSProperties => {
  switch (type) {
    case "vip":       return { background: "linear-gradient(135deg,#f5af19,#f12711)", color: "#fff" };
    case "active":    return { background: "#d4edda", color: "#155724" };
    case "demo":      return { background: "#e2e3f3", color: "#383d8b" };
    case "expired":   return { background: "#f8d7da", color: "#721c24" };
    case "read-only": return { background: "#fff3cd", color: "#856404" };
    case "basic":     return { background: "#e9ecef", color: "#495057" };
    case "standard":  return { background: "#cce5ff", color: "#004085" };
    case "premium":   return { background: "#d4edda", color: "#155724" };
    case "ΤΙΜΟΛΟΓΙΟ": return { background: "#e3f2fd", color: "#1565c0" };
    case "ΑΠΟΔΕΙΞΗ":  return { background: "#fff3e0", color: "#e65100" };
    default:          return { background: "#e9ecef", color: "#495057" };
  }
};
```

---

## Функционал

### 1. VIP Активация

**Форма:**
- Email пользователя (поиск в базе)
- Срок действия:
  - `forever` — +100 лет (бессрочно)
  - `1`, `3`, `6`, `12` — месяцы
  - `custom` — указать дату
- Причина (опционально)

**При активации обновляются поля:**
```sql
UPDATE profiles SET
  subscription_status = 'vip',
  subscription_plan = 'VIP',
  vip_expires_at = <дата>,
  vip_granted_by = <UUID админа>,
  vip_reason = <причина>
WHERE id = <user_id>;
```

**Бессрочный VIP:**
```typescript
const d = new Date();
d.setFullYear(d.getFullYear() + 100); // +100 лет
expiresAt = d.toISOString();
```

**Email уведомление:** отправляется через `/api/email/vip-activated`

### 2. Удаление VIP

```sql
UPDATE profiles SET
  subscription_status = 'active',
  subscription_plan = NULL,
  vip_expires_at = NULL,
  vip_granted_by = NULL,
  vip_reason = NULL
WHERE id = <user_id>;
```

### 3. Редактирование пользователя

**Редактируемые поля:**
- `name` — имя
- `email` — email
- `phone` — телефон
- `doc_type` — ΑΠΟΔΕΙΞΗ / ΤΙΜΟΛΟΓΙΟ
- `subscription_status` — статус
- `subscription_plan` — план

**Поля ΤΙΜΟΛΟΓΙΟ (если `doc_type = "ΤΙΜΟΛΟΓΙΟ"`):**
- `afm` — ΑΦΜ
- `doy` — ΔΟΥ
- `company_name` — название компании
- `company_activity` — вид деятельности
- `company_address` — адрес

### 4. Excel Экспорт

**Библиотека:** `xlsx` (динамический импорт)

```typescript
const exportToExcel = async (data: Record<string, unknown>[], filename: string) => {
  const XLSX = await import("xlsx");
  const ws = XLSX.utils.json_to_sheet(data);
  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, "Data");
  XLSX.writeFile(wb, `${filename}.xlsx`);
};
```

**Файлы экспорта:**
- `APALLAKTIS_Users.xlsx` — все пользователи (с фильтрами)
- `APALLAKTIS_Referrals.xlsx` — статистика рефералов
- `APALLAKTIS_VIP.xlsx` — VIP пользователи

---

## Дизайн-система

### Цвета

```css
/* Основные */
--bg-dark: #1a1a2e;
--bg-dark-secondary: #16213e;
--bg-light: #fff;
--bg-section: #f8f9fa;
--border: #e9ecef;
--text-primary: #1a1a2e;
--text-secondary: #495057;
--accent: #FF6B35;

/* Кнопки */
--btn-danger: #e74c3c;
--btn-success: linear-gradient(135deg, #11998e, #38ef7d);
--btn-vip: linear-gradient(135deg, #f5af19, #f12711);
--btn-save: linear-gradient(135deg, #FF6B35, #F7C59F);
```

### Градиенты карточек статистики

```css
/* Зелёный */
linear-gradient(135deg, #11998e, #38ef7d)

/* Голубой */
linear-gradient(135deg, #4facfe, #00f2fe)

/* Розово-жёлтый */
linear-gradient(135deg, #fa709a, #fee140)

/* Фиолетово-розовый */
linear-gradient(135deg, #a18cd1, #fbc2eb)

/* Фиолетовый */
linear-gradient(135deg, #667eea, #764ba2)

/* Розово-красный */
linear-gradient(135deg, #f093fb, #f5576c)

/* VIP (оранжево-красный) */
linear-gradient(135deg, #f5af19, #f12711)
```

### Шрифт

```css
font-family: 'Manrope', sans-serif;
```

### Компоненты

#### Header
```css
background: #1a1a2e;
padding: 15px 40px;
position: sticky;
top: 0;
z-index: 100;
```

#### Карточка статистики
```css
border-radius: 12px;
padding: 20px;
color: #fff;
text-align: center;
/* + градиентный background */
```

#### Section (раскрывающаяся секция)
```css
background: #f8f9fa;
border-radius: 16px;
border: 2px solid #e9ecef;
overflow: hidden;
margin-bottom: 25px;
```

#### Заголовок секции
```css
background: linear-gradient(135deg, #1a1a2e, #16213e);
color: #fff;
padding: 18px 25px;
cursor: pointer;
```

#### Modal
```css
/* Overlay */
position: fixed;
top: 0; left: 0; right: 0; bottom: 0;
background: rgba(0,0,0,0.5);
z-index: 1000;

/* Content */
background: #fff;
border-radius: 16px;
padding: 25px;
max-width: 600px;
max-height: 90vh;
overflow-y: auto;
```

#### Badge
```css
display: inline-block;
padding: 2px 6px;
border-radius: 10px;
font-size: 9px;
font-weight: 700;
text-transform: uppercase;
```

#### Input / Select
```css
padding: 8px;
border: 2px solid #e9ecef;
border-radius: 6px;
font-size: 13px;
width: 100%;
```

#### Button (Excel)
```css
background: linear-gradient(135deg, #11998e, #38ef7d);
color: #fff;
border: none;
padding: 10px 20px;
border-radius: 8px;
font-weight: 700;
cursor: pointer;
```

---

## API Endpoints

### POST `/api/email/vip-activated`

Отправляет email уведомление при активации VIP.

**Request:**
```typescript
{
  userId: string;      // UUID пользователя
  userEmail: string;   // Email пользователя
  userName: string;    // Имя пользователя
  expiresAt: string;   // ISO дата истечения VIP
  reason?: string;     // Причина выдачи (опционально)
  locale: string;      // Язык ("el", "en", "ru", etc.)
}
```

**Response:**
```typescript
{ success: true }
// или
{ error: "Error message" }
```

### POST `/api/email/vip-cancelled`

Отправляет email при отзыве VIP (не используется в текущей версии).

---

## Компоненты

### Section

Раскрывающаяся секция с заголовком.

```tsx
interface SectionProps {
  title: string;        // Заголовок
  icon: string;         // Эмодзи иконка
  isOpen: boolean;      // Состояние (открыта/закрыта)
  onToggle: () => void; // Callback переключения
  children: ReactNode;  // Содержимое
}

function Section({ title, icon, isOpen, onToggle, children }: SectionProps) {
  return (
    <section style={{ /* стили */ }}>
      <div onClick={onToggle} style={{ /* заголовок */ }}>
        <h2>{icon} {title}</h2>
        <span style={{ transform: isOpen ? "rotate(180deg)" : "rotate(0deg)" }}>▼</span>
      </div>
      {isOpen && <div style={{ /* контент */ }}>{children}</div>}
    </section>
  );
}
```

### Modal

Модальное окно с overlay.

```tsx
interface ModalProps {
  children: ReactNode;
  onClose: () => void;
}

function Modal({ children, onClose }: ModalProps) {
  return (
    <div onClick={onClose} style={{ /* overlay */ }}>
      <div onClick={(e) => e.stopPropagation()} style={{ /* content */ }}>
        {children}
      </div>
    </div>
  );
}
```

---

## Безопасность

### Проверка роли

```typescript
// При загрузке страницы
const { data: profile } = await supabase
  .from("profiles")
  .select("role")
  .eq("id", user.id)
  .single();

if (!profile || profile.role !== "admin") {
  router.push(`/${locale}`); // Редирект для не-админов
  return;
}
```

### Layout редирект

**Файл:** `frontend/app/[locale]/admin/layout.tsx`

Редиректит все локали кроме `/ru/admin` на `/ru/admin` (админка только на русском).

---

## Зависимости

```json
{
  "@supabase/supabase-js": "^2.x",
  "next": "^16.x",
  "react": "^19.x",
  "xlsx": "^0.18.x"
}
```

---

## Примеры использования

### Выдача бессрочного VIP

1. Открыть секцию ⭐ VIP
2. Нажать "⭐ Добавить VIP"
3. Ввести email пользователя
4. Выбрать "Бессрочно (+100 лет)"
5. Указать причину (опционально)
6. Нажать "Активировать"

### Поиск пользователя по ΑΦΜ

1. Открыть секцию 👥 Пользователи
2. Выбрать фильтр "Документ" → "ΤΙΜΟΛΟΓΙΟ"
3. Ввести ΑΦΜ в поле поиска
4. Результат отфильтруется автоматически

### Экспорт всех VIP в Excel

1. Открыть секцию ⭐ VIP
2. Нажать "📥 Excel"
3. Файл `APALLAKTIS_VIP.xlsx` скачается автоматически

---

*Документация создана: 2025-01-25*
*Версия: 1.0*
