# 🚀 НАЧАЛО РАБОТЫ - Простая инструкция

## Шаг 1: Запустить Docker (PostgreSQL + n8n)

Открой терминал в папке проекта и выполни:

```bash
docker-compose up -d
```

Подожди 30-60 секунд пока все запустится.

**Проверь что запустилось:**
```bash
docker ps
```

Должно показать 2 контейнера:
- `apallaktis-db` (PostgreSQL)
- `apallaktis-n8n` (n8n)

---

## Шаг 2: Открыть n8n

Открой в браузере: **http://localhost:5678**

**Логин:**
- Username: `admin`
- Password: `admin123`

---

## Шаг 3: Импортировать Workflows в n8n

### 3.1 Импорт Registration Workflow

1. В n8n нажми **"Add workflow"** (или кнопку "+")
2. Нажми на **три точки** (⋮) → **Import from File**
3. Выбери файл: `n8n/updated-registration-workflow.json`
4. Нажми **Save** (кнопка Save вверху справа)

### 3.2 Импорт Login Workflow

1. Снова нажми **"Add workflow"** (или вернись на главную и нажми "+")
2. **Три точки** (⋮) → **Import from File**
3. Выбери файл: `n8n/login-workflow.json`
4. Нажми **Save**

---

## Шаг 4: Настроить PostgreSQL Credential в n8n

### Для КАЖДОГО workflow:

1. Открой workflow (Registration или Login)
2. Найди узел **"PostgreSQL"** (любой из них)
3. Кликни на него
4. В секции **"Credential to connect with"** нажми **"Create New"**
5. Заполни:
   - **Host**: `postgres`
   - **Database**: `apallaktis`
   - **User**: `apallaktis`
   - **Password**: `apallaktis_password_change_me`
   - **Port**: `5432`
   - **SSL**: OFF
6. Нажми **Save**
7. **ВАЖНО:** Нажми кнопку **"Active"** (вверху справа) чтобы активировать workflow!

**Повтори для второго workflow!**

---

## Шаг 5: Запустить Frontend

Открой **НОВЫЙ** терминал и выполни:

```bash
cd frontend
npm install
npm run dev
```

Подожди пока соберется. Откроется на: **http://localhost:3000**

---

## Шаг 6: ТЕСТИРУЕМ! 🎉

### Тест 1: Регистрация

1. Открой **http://localhost:3000**
2. Выбери язык (например, Ελληνικά)
3. Нажми **"Δημιουργία λογαριασμού"**
4. Заполни форму:
   - Имя: **Test User**
   - Email: **test@example.com**
   - Пароль: **123456**
   - Подтверждение пароля: **123456**
   - Телефон: **1234567890**
   - Поставь галочку согласия
5. Нажми **"Εγγραφή"**

**Если все работает:** попадешь на страницу "Спасибо за регистрацию!"

---

### Тест 2: Вход

1. Открой **http://localhost:3000/el/login**
2. Введи:
   - Email: **test@example.com**
   - Пароль: **123456**
3. Нажми **"Σύνδεση"**

**Если все работает:** попадешь на Dashboard с твоими данными!

---

### Тест 3: Logout

1. На Dashboard нажми кнопку **"Logout"**
2. Должен вернуться на главную страницу

---

## ❌ Если что-то не работает

### Ошибка "Connection refused" или "Network error"

**Проверь что Docker контейнеры запущены:**
```bash
docker ps
```

**Если не запущены, запусти заново:**
```bash
docker-compose down
docker-compose up -d
```

---

### Ошибка "Invalid credentials" при входе

**Проверь в n8n:**
1. Оба workflow **Active** (зеленая кнопка вверху)?
2. PostgreSQL credentials настроены правильно?
3. В Registration workflow - пользователь создался?

**Проверить в базе данных:**
```bash
docker exec -it apallaktis-db psql -U apallaktis -d apallaktis -c "SELECT * FROM users;"
```

Должен показать твоего пользователя.

---

### n8n показывает ошибки в workflow

**Самая частая причина:** не настроены PostgreSQL credentials

1. Открой workflow
2. Кликни на узел с ошибкой (красный!)
3. Проверь что PostgreSQL credential настроен
4. Попробуй нажать **"Execute node"** для теста

---

## 📞 Остановить все

```bash
# Остановить Docker
docker-compose down

# Остановить frontend
Ctrl+C в терминале
```

---

## 🎯 Следующие шаги

После успешного тестирования:
- [ ] Поменять пароли в docker-compose.yml
- [ ] Настроить email отправку (SMTP)
- [ ] Добавить Telegram уведомления (опционально)
- [ ] Продолжить разработку (DEMO режим, объекты, и т.д.)

---

**Если застряла на любом шаге - пиши мне, помогу!** 🚀
