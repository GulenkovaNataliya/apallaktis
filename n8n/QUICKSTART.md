# ⚡ Быстрый старт - 5 минут

## 🚀 Запуск за 3 команды

```bash
# 1. Перейдите в директорию n8n
cd n8n

# 2. Скопируйте настройки
cp .env.example .env

# 3. Запустите всё
docker-compose up -d
```

**Готово!** n8n запущен на http://localhost:5678

---

## 📝 Настройка за 5 шагов

### Шаг 1: Откройте n8n
Перейдите на http://localhost:5678

**Логин:** admin
**Пароль:** admin (измените в `.env`)

---

### Шаг 2: Импортируйте workflow
1. Нажмите **"Import from File"** в правом верхнем углу
2. Выберите файл `customer-registration-workflow.json`
3. Нажмите **"Import"**

---

### Шаг 3: Настройте PostgreSQL credentials
1. Откройте workflow
2. Нажмите на ноду **"Save to PostgreSQL"**
3. В разделе **Credentials** нажмите **"Create New"**
4. Заполните:
   - **Host:** `postgres` (если через Docker) или `localhost`
   - **Database:** `apallaktis`
   - **User:** `apallaktis_user`
   - **Password:** `changeme` (или из вашего `.env`)
   - **Port:** `5432`
5. Нажмите **"Save"**

---

### Шаг 4: Настройте Email (SMTP)
1. Нажмите на ноду **"Send Welcome Email"**
2. В разделе **Credentials** нажмите **"Create New"**
3. Для Gmail:
   - **Host:** `smtp.gmail.com`
   - **Port:** `587`
   - **User:** ваш email
   - **Password:** [App Password](https://myaccount.google.com/apppasswords)
   - **From Email:** ваш email
4. Нажмите **"Save"**

> **Примечание:** Для других SMTP провайдеров см. раздел "Email Configuration" в README.md

---

### Шаг 5: Активируйте workflow
1. В правом верхнем углу переключите **"Inactive"** → **"Active"**
2. Скопируйте Webhook URL (показан в ноде **"Webhook - Registration Form"**)
3. URL будет вида: `http://localhost:5678/webhook/register`

---

## 🔗 Подключение к Frontend

### 1. Создайте `.env.local` в папке frontend:
```bash
cd ../frontend
cp .env.local.example .env.local
```

### 2. Отредактируйте `.env.local`:
```bash
NEXT_PUBLIC_N8N_WEBHOOK_URL=http://localhost:5678/webhook/register
```

### 3. Перезапустите frontend:
```bash
npm run dev
```

---

## ✅ Проверка

### Тест 1: Отправьте форму
1. Откройте http://localhost:3000/el/register
2. Заполните форму
3. Нажмите "ΕΓΓΡΑΦΗ"

### Тест 2: Проверьте БД
```bash
cd n8n
docker-compose exec postgres psql -U apallaktis_user -d apallaktis -c "SELECT * FROM customers;"
```

### Тест 3: Проверьте email
- Проверьте почту, указанную при регистрации
- Должно прийти приветственное письмо

---

## 🔧 Опциональные настройки

### Telegram уведомления (необязательно)
1. Создайте бота через @BotFather
2. Получите Bot Token
3. Узнайте Chat ID через @userinfobot
4. В n8n нажмите на ноду **"Notify Telegram"**
5. Создайте Telegram credentials с вашим Bot Token
6. В ноде укажите ваш Chat ID

---

## 🐛 Проблемы?

### n8n не открывается
```bash
docker-compose ps
docker-compose logs n8n
```

### PostgreSQL ошибка подключения
```bash
docker-compose logs postgres
```

### Email не отправляется
- Проверьте SMTP credentials
- Для Gmail используйте [App Password](https://myaccount.google.com/apppasswords)
- Проверьте логи: `docker-compose logs n8n`

---

## 📊 Полезные команды

```bash
# Посмотреть логи
docker-compose logs -f

# Остановить
docker-compose down

# Перезапустить
docker-compose restart

# Удалить всё (включая данные)
docker-compose down -v
```

---

## 🎉 Готово!

Ваша система регистрации работает! Каждая новая регистрация:
- ✅ Сохраняется в PostgreSQL
- ✅ Отправляет email клиенту
- ✅ Уведомляет вас в Telegram (если настроено)

**Next steps:** Откройте [README.md](./README.md) для полной документации
