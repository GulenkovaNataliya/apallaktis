# 🚀 Быстрый старт: Админ-панель

**За 5 минут до первого входа в админ-панель**

---

## Шаг 1: Применить миграции в Supabase (3 мин)

### 1.1 Откройте Supabase Dashboard

Перейдите по ссылке: https://gnuivvrpdibgwmhbfqxv.supabase.co

### 1.2 Откройте SQL Editor

В боковом меню выберите **SQL Editor**

### 1.3 Применить миграцию #1: Роль админа

1. Откройте файл: `database/migrations/add_admin_role.sql`
2. Скопируйте **всё содержимое** файла
3. Вставьте в SQL Editor
4. Нажмите **Run** (или Ctrl+Enter)
5. Дождитесь сообщения "Success"

### 1.4 Применить миграцию #2: Логирование

1. Откройте файл: `database/migrations/create_admin_actions_log.sql`
2. Скопируйте **всё содержимое** файла
3. Вставьте в SQL Editor
4. Нажмите **Run** (или Ctrl+Enter)
5. Дождитесь сообщения "Success"

---

## Шаг 2: Проверка (1 мин)

Выполните в SQL Editor следующий запрос:

```sql
SELECT email, role FROM profiles WHERE email = 'gulenkovanatalia@gmail.com';
```

**Ожидаемый результат:**
```
email                      | role
---------------------------+-------
gulenkovanatalia@gmail.com | admin
```

Если видите `admin` - всё готово! ✅

---

## Шаг 3: Запуск админ-панели (1 мин)

### 3.1 Войдите в систему

1. Откройте: http://localhost:3000/el
2. Войдите с вашим email: `gulenkovanatalia@gmail.com`
3. Введите пароль

### 3.2 Откройте админ-панель

Перейдите по адресу: http://localhost:3000/el/admin

**Вы должны увидеть:**
- 📊 Панель администратора
- Статистику пользователей
- Кнопки навигации

---

## 🎯 Что можно делать?

### 👥 Управление пользователями

**URL**: `/el/admin/users`

**Функции:**
- 🔍 Поиск по email/имени/номеру аккаунта
- 🎛️ Фильтр по статусу (DEMO/Active/Expired/VIP)
- ➕ Продлить подписку (+1 месяц)
- 🚫 Заблокировать пользователя
- 🔓 Разблокировать пользователя
- ⭐ Активировать VIP

### ⭐ Активация VIP

**URL**: `/el/admin/vip`

**Функции:**
- Ввод email пользователя
- Выбор срока: 1м/3м/6м/1г/навсегда/своя дата
- Причина (опционально)
- Просмотр всех VIP пользователей
- Отзыв VIP

---

## ⚠️ Если что-то не работает

### Проблема: "Access Denied" или редирект на главную

**Решение:**
1. Проверьте, что миграция #1 применена
2. Проверьте роль в базе данных:
   ```sql
   SELECT email, role FROM profiles WHERE email = 'gulenkovanatalia@gmail.com';
   ```
3. Если роль не `admin`, выполните:
   ```sql
   UPDATE profiles SET role = 'admin' WHERE email = 'gulenkovanatalia@gmail.com';
   ```

### Проблема: Middleware error

**Решение:**
1. Проверьте что файл `frontend/middleware.ts` существует (не `.disabled`)
2. Перезапустите dev сервер: `npm run dev`

### Проблема: Таблица admin_actions_log не найдена

**Решение:**
1. Проверьте, что миграция #2 применена
2. Выполните запрос:
   ```sql
   SELECT * FROM admin_actions_log LIMIT 1;
   ```
3. Если ошибка - повторно примените миграцию

---

## 📝 Дополнительные настройки (опционально)

### Service Role Key для логирования

Для полноценного логирования админ-действий добавьте в `.env.local`:

```env
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key_здесь
```

**Где взять ключ:**
1. Supabase Dashboard → Settings → API
2. Скопируйте **service_role key** (secret)
3. Вставьте в `.env.local`
4. Перезапустите сервер

---

## 🎉 Готово!

Теперь у вас есть полноценная админ-панель для управления пользователями и активации VIP!

**Полезные ссылки:**
- 📊 Админ-панель: http://localhost:3000/el/admin
- 👥 Пользователи: http://localhost:3000/el/admin/users
- ⭐ VIP: http://localhost:3000/el/admin/vip
- 📚 Подробный отчет: `PHASE_10_COMPLETION_REPORT.md`

---

**Последнее обновление**: 2026-01-09
