# 🚀 Настройка Supabase для Apallaktis

## Шаг 1: Создание проекта Supabase (5 минут)

1. **Перейдите на** https://supabase.com
2. **Нажмите "Start your project"** (или "Sign in" если уже есть аккаунт)
3. **Войдите через GitHub** (самый простой способ)
4. **Создайте новый проект:**
   - Нажмите "+ New project"
   - Название проекта: `apallaktis` (или как хотите)
   - Database Password: придумайте сложный пароль (сохраните его!)
   - Регион: **Europe West (Ireland)** - ближайший к Греции
   - Plan: **Free** (достаточно для начала)
   - Нажмите "Create new project"

5. **Подождите 2-3 минуты** пока проект создается ☕

---

## Шаг 2: Получение API ключей (1 минута)

1. **После создания проекта** перейдите в:
   - Settings (⚙️ слева внизу)
   - API

2. **Скопируйте два значения:**
   - **Project URL** - например: `https://xyzcompany.supabase.co`
   - **anon public** ключ - длинная строка типа `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`

---

## Шаг 3: Настройка переменных окружения (1 минута)

1. **Создайте файл** `frontend/.env.local` (если его нет)
2. **Вставьте:**

```env
NEXT_PUBLIC_SUPABASE_URL=https://ваш-проект.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-ключ
```

3. **Замените значения** на свои из Шага 2
4. **Сохраните файл**

⚠️ **ВАЖНО:** Файл `.env.local` не должен попадать в Git! Он уже добавлен в `.gitignore`

---

## Шаг 4: Настройка Email подтверждения (3 минуты)

1. **Перейдите в:**
   - Authentication (слева)
   - Providers
   - Email

2. **Убедитесь что включено:**
   - ✅ Enable Email provider
   - ✅ Confirm email (должно быть включено!)

3. **Настройте шаблон письма:**
   - Перейдите в: Authentication → Email Templates
   - Выберите "Confirm signup"
   - **Замените текст на греческий** (опционально):

```html
<h2>Επιβεβαίωση Email</h2>
<p>Ακολουθήστε αυτόν τον σύνδεσμο για να επιβεβαιώσετε το email σας:</p>
<p><a href="{{ .ConfirmationURL }}">Επιβεβαίωση Email</a></p>
```

4. **Сохраните** (Save)

---

## Шаг 5: Настройка URL переадресации (2 минуты)

1. **Перейдите в:**
   - Authentication → URL Configuration

2. **Добавьте в Site URL:**
   - Для локальной разработки: `http://localhost:3000`
   - Для продакшена: `https://ваш-домен.com`

3. **Добавьте в Redirect URLs:**
   ```
   http://localhost:3000/**
   https://ваш-домен.com/**
   ```

4. **Сохраните**

---

## Шаг 6: Создание таблицы пользователей (5 минут)

1. **Перейдите в:**
   - SQL Editor (слева)
   - "+ New query"

2. **Вставьте SQL:**

```sql
-- Создаем таблицу профилей пользователей
create table public.profiles (
  id uuid references auth.users on delete cascade primary key,
  name text,
  phone text,
  country_code text,
  invoice_type text check (invoice_type in ('receipt', 'invoice')),
  company_name text,
  afm text,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  updated_at timestamp with time zone default timezone('utc'::text, now()) not null
);

-- Включаем Row Level Security
alter table public.profiles enable row level security;

-- Политика: пользователи могут видеть только свой профиль
create policy "Users can view own profile"
  on public.profiles for select
  using (auth.uid() = id);

-- Политика: пользователи могут обновлять только свой профиль
create policy "Users can update own profile"
  on public.profiles for update
  using (auth.uid() = id);

-- Функция: автоматическое создание профиля при регистрации
create or replace function public.handle_new_user()
returns trigger as $$
begin
  insert into public.profiles (id, name, phone, country_code, invoice_type, company_name, afm)
  values (
    new.id,
    new.raw_user_meta_data->>'name',
    new.raw_user_meta_data->>'phone',
    new.raw_user_meta_data->>'country_code',
    new.raw_user_meta_data->>'invoice_type',
    new.raw_user_meta_data->>'company_name',
    new.raw_user_meta_data->>'afm'
  );
  return new;
end;
$$ language plpgsql security definer;

-- Триггер: вызов функции после регистрации
create trigger on_auth_user_created
  after insert on auth.users
  for each row execute function public.handle_new_user();
```

3. **Нажмите "Run" (Ctrl+Enter)**
4. **Должно появиться:** "Success. No rows returned"

---

## Шаг 7: Проверка (2 минуты)

1. **Запустите приложение:**
   ```bash
   cd frontend
   npm run dev
   ```

2. **Откройте:** http://localhost:3000/el

3. **Попробуйте зарегистрироваться:**
   - Перейдите на страницу регистрации
   - Заполните форму
   - Нажмите "Εγγραφή"

4. **Проверьте email:**
   - Откройте почту (gmail, outlook и т.д.)
   - Найдите письмо от Supabase
   - Нажмите на ссылку подтверждения

5. **Войдите в систему:**
   - Вернитесь на страницу логина
   - Введите email и пароль
   - Должна открыться страница page-pay!

---

## ✅ Готово!

Теперь у вас работает:
- ✅ Регистрация пользователей
- ✅ Email подтверждение (автоматически!)
- ✅ Вход в систему
- ✅ Безопасное хранение паролей
- ✅ Сохранение данных профиля (имя, телефон, ΑΦΜ и т.д.)

---

## 📊 Просмотр пользователей

**Чтобы увидеть зарегистрированных пользователей:**

1. Откройте: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в: Authentication → Users
4. Здесь все пользователи!

---

## 🔧 Дополнительные настройки

### Отключить подтверждение email (для тестирования)

Если хотите тестировать БЕЗ подтверждения email:

1. Authentication → Providers → Email
2. Выключите "Confirm email"
3. Сохраните

⚠️ **Не рекомендуется для продакшена!**

### Настроить свой SMTP (для продакшена)

По умолчанию Supabase использует свой SMTP (максимум 3 письма в час).

Для продакшена подключите свой SMTP:
1. Settings → Auth → Email Auth
2. Enable Custom SMTP
3. Введите данные вашего SMTP сервера

**Популярные SMTP:**
- Gmail (бесплатно, 500 писем/день)
- SendGrid (бесплатно, 100 писем/день)
- Mailgun (бесплатно, 5000 писем/месяц)

---

## 🆘 Проблемы?

### Ошибка "Invalid API key"
- Проверьте `.env.local`
- Убедитесь что скопировали правильные ключи
- Перезапустите `npm run dev`

### Email не приходит
- Проверьте спам
- Подождите 2-3 минуты
- Проверьте что "Confirm email" включено
- Для тестирования можно отключить подтверждение

### Ошибка при входе
- Убедитесь что подтвердили email
- Проверьте правильность пароля
- Посмотрите консоль браузера (F12) на ошибки

---

## 📱 Следующий шаг: SMS подтверждение

Хотите добавить SMS? Смотрите `SMS_SETUP.md`

---

**Создано:** 2025-12-24
**Версия:** 1.0
