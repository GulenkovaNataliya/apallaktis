# 🔐 Инструкция по настройке авторизации

## ✅ Что сделано

### Frontend
1. **Форма регистрации** - добавлены поля password/confirmPassword на всех 8 языках
2. **Форма входа** - полноценная страница login с обработкой ошибок
3. **Auth Context** - управление состоянием авторизации по всему приложению
4. **Dashboard** - тестовая страница с отображением данных пользователя и кнопкой logout

### Backend (n8n)
1. **Database schema** - полная схема БД с таблицей users и всеми необходимыми полями
2. **Login workflow** - проверка credentials, генерация JWT токена
3. **Updated Registration workflow** - хэширование пароля (bcrypt), создание пользователя, DEMO режим

---

## 📋 Что нужно сделать для запуска

### 1. Настроить PostgreSQL

```bash
# Создать базу данных
createdb apallaktis

# Импортировать схему
psql apallaktis < database/schema.sql
```

### 2. Настроить n8n

#### Установка и запуск n8n:
```bash
# Вариант А: Docker (рекомендуется)
cd n8n
docker-compose up -d

# Вариант Б: npm
npm install -g n8n
n8n start
```

#### Импорт workflows:
1. Открыть n8n (обычно http://localhost:5678)
2. Импортировать **login-workflow.json**
3. Импортировать **updated-registration-workflow.json**

#### Настроить credentials в n8n:

**PostgreSQL:**
- Host: localhost (или ваш DB host)
- Port: 5432
- Database: apallaktis
- User: postgres
- Password: ваш_пароль

**SMTP (для welcome emails):**
- Вариант А: Gmail
  - Host: smtp.gmail.com
  - Port: 587
  - User: ваш_email@gmail.com
  - Password: App Password (нужно создать в Google Account)

- Вариант Б: SendGrid (бесплатно до 100 писем/день)
  - Host: smtp.sendgrid.net
  - Port: 587
  - User: apikey
  - Password: ваш_sendgrid_api_key

**Telegram (опционально):**
- Bot Token: получить у @BotFather
- Chat ID: ваш chat ID

**JWT Secret:**
В n8n добавить environment variable:
```
JWT_SECRET=ваш_секретный_ключ_минимум_32_символа
```

### 3. Настроить frontend environment variables

Создать файл **frontend/.env.local**:

```env
# n8n webhooks
NEXT_PUBLIC_N8N_WEBHOOK_URL=http://localhost:5678/webhook/register
NEXT_PUBLIC_N8N_LOGIN_URL=http://localhost:5678/webhook/login
```

### 4. Запустить frontend

```bash
cd frontend
npm install
npm run dev
```

Приложение откроется на http://localhost:3000

---

## 🧪 Тестирование

### Тест 1: Регистрация
1. Открыть http://localhost:3000
2. Выбрать язык
3. Нажать "Δημιουργία λογαριασμού"
4. Заполнить форму:
   - Имя
   - Email
   - **Пароль** (минимум 6 символов)
   - **Подтверждение пароля**
   - Телефон
   - Галочка согласия
5. Нажать "Εγγραφή"
6. Должен произойти редирект на /thank-you

**Проверить в БД:**
```sql
SELECT id, email, name, subscription_status, demo_expires_at, referral_code
FROM users
ORDER BY created_at DESC
LIMIT 1;
```

### Тест 2: Login
1. Открыть http://localhost:3000/el/login
2. Ввести email и пароль
3. Нажать "Σύνδεση"
4. Должен произойти редирект на /dashboard
5. Dashboard показывает данные пользователя:
   - Имя, email, телефон
   - Subscription status (DEMO)
   - DEMO expires in (48 часов)
   - Referral code

### Тест 3: Logout
1. На dashboard нажать кнопку "Logout"
2. Должен произойти редирект на главную страницу
3. При попытке зайти на /dashboard должен редиректить на /login

---

## 📂 Структура файлов

```
apallaktis/
├── database/
│   └── schema.sql ✅ Схема БД
│
├── n8n/
│   ├── login-workflow.json ✅ Workflow для входа
│   └── updated-registration-workflow.json ✅ Workflow для регистрации
│
└── frontend/
    ├── app/
    │   └── [locale]/
    │       ├── login/
    │       │   └── page.tsx ✅ Страница входа
    │       ├── register/
    │       │   └── page.tsx ✅ Форма регистрации (с паролем)
    │       ├── dashboard/
    │       │   └── page.tsx ✅ Dashboard с logout
    │       └── layout.tsx ✅ С AuthProvider
    │
    ├── lib/
    │   ├── auth-context.tsx ✅ Auth context + hooks
    │   └── messages.ts ✅ Переводы (включая password)
    │
    └── types/
        └── user.ts ✅ TypeScript типы для User
```

---

## 🔐 Безопасность

### Что уже реализовано:
- ✅ Пароли хэшируются с bcrypt (saltRounds: 10)
- ✅ JWT токены с expiration (30 дней)
- ✅ Валидация email
- ✅ Валидация пароля (минимум 6 символов)
- ✅ Проверка на дубликаты email
- ✅ Demo expires автоматически через 48 часов
- ✅ Referral code генерируется автоматически

### TODO (для продакшена):
- [ ] HTTPS обязательно!
- [ ] HttpOnly cookies вместо localStorage для JWT
- [ ] Rate limiting на login endpoint
- [ ] Email verification
- [ ] Password reset функциональность
- [ ] 2FA (опционально)

---

## ⚙️ Environment Variables

### Frontend (.env.local):
```env
NEXT_PUBLIC_N8N_WEBHOOK_URL=http://localhost:5678/webhook/register
NEXT_PUBLIC_N8N_LOGIN_URL=http://localhost:5678/webhook/login
```

### n8n (environment):
```env
JWT_SECRET=your-super-secret-key-change-in-production
TELEGRAM_CHAT_ID=your_telegram_chat_id (опционально)
```

---

## 🐛 Troubleshooting

### "Invalid credentials" при логине
- Проверьте, что пользователь зарегистрирован
- Проверьте правильность email/password
- Проверьте n8n logs для ошибок

### "Registration error"
- Проверьте подключение к PostgreSQL
- Проверьте что schema импортирована
- Email уже зарегистрирован?

### Dashboard не показывается
- Проверьте localStorage (authToken должен быть)
- Проверьте консоль браузера на ошибки
- Попробуйте logout и login заново

### DEMO expires неправильно считается
- Проверьте timezone на сервере
- Проверьте поле demo_expires_at в БД
- Должно быть автоматически +48 часов от created_at

---

## 📞 Что дальше?

После успешного тестирования:

1. **Фаза 3.2**: DEMO режим
   - [ ] Ограничение на 3 объекта
   - [ ] Предупреждение за 6 часов до истечения
   - [ ] Grace period (read-only после истечения)

2. **Production deployment**:
   - [ ] Купить домен
   - [ ] Настроить hosting (Vercel + Railway)
   - [ ] Настроить HTTPS
   - [ ] Обновить environment variables
   - [ ] Импортировать workflows на production n8n

3. **Дополнительно**:
   - [ ] Password reset (forgot password)
   - [ ] Email verification
   - [ ] Social login (Google, Facebook)

---

**Дата создания:** 2025-12-18
**Версия:** 1.0
